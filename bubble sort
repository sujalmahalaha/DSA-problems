#include <bits/stdc++.h> 
void bubbleSort(vector<int>& arr, int n)
{  

    for(int i = 0 ; i <n-1; i++){
// inner loop check every pairs and update .
        for(int j = 0; j<n-i-1; j++){
            if(arr[j]>arr[j+1]){
                swap(arr[j], arr[j+1]);
            }
        }
    }
}
